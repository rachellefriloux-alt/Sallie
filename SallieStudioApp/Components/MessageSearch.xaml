<UserControl
    x:Class="SallieStudioApp.Components.MessageSearch"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource WindowBackground}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search Header -->
        <Border Grid.Row="0"
                Background="{StaticResource PrimaryBrush}"
                Padding="20,16"
                CornerRadius="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search Input -->
                <Border Grid.Column="0"
                        Background="White"
                        CornerRadius="20"
                        Padding="16,12"
                        Margin="0,0,12,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                                   Text="🔍"
                                   FontSize="16"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"/>

                        <TextBox x:Name="SearchInputTextBox"
                                 Grid.Column="1"
                                 Text="{Binding SearchQuery, Mode=TwoWay}"
                                 PlaceholderText="Search messages..."
                                 Background="Transparent"
                                 BorderThickness="0"
                                 FontSize="14"
                                 VerticalAlignment="Center"
                                 KeyUp="SearchInputTextBox_KeyUp"/>

                        <Button Grid.Column="2"
                                x:Name="ClearSearchButton"
                                Content="✕"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="8"
                                FontSize="14"
                                Foreground="#6b7280"
                                Click="ClearSearchButton_Click"
                                Visibility="{Binding HasSearchQuery, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </Grid>
                </Border>

                <!-- Filter Button -->
                <Button Grid.Column="1"
                        x:Name="FilterButton"
                        Content="⚙️ Filters"
                        Background="White"
                        Foreground="{StaticResource TextPrimary}"
                        BorderThickness="0"
                        Padding="16,8"
                        FontSize="14"
                        Click="FilterButton_Click"/>
            </Grid>

            <!-- Quick Filters -->
            <StackPanel Grid.Column="0"
                        Orientation="Horizontal"
                        Margin="0,12,0,0">
                <Button x:Name="AllFilterButton"
                        Content="All"
                        Background="White"
                        Foreground="{StaticResource TextPrimary}"
                        BorderThickness="0"
                        Padding="12,6"
                        FontSize="12"
                        Margin="0,0,8,0"
                        Click="FilterButton_Click"
                        Tag="all"/>
                
                <Button x:Name="FromUserFilterButton"
                        Content="From You"
                        Background="White"
                        Foreground="{StaticResource TextPrimary}"
                        BorderThickness="0"
                        Padding="12,6"
                        FontSize="12"
                        Margin="0,0,8,0"
                        Click="FilterButton_Click"
                        Tag="user"/>
                
                <Button x:Name="FromSallieFilterButton"
                        Content="From Sallie"
                        Background="White"
                        Foreground="{StaticResource TextPrimary}"
                        BorderThickness="0"
                        Padding="12,6"
                        FontSize="12"
                        Margin="0,0,8,0"
                        Click="FilterButton_Click"
                        Tag="sallie"/>
                
                <!-- Category filters will be added dynamically -->
                <ItemsControl x:Name="CategoryFiltersContainer"
                              ItemsSource="{Binding Categories}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Name}"
                                    Background="White"
                                    Foreground="{StaticResource TextPrimary}"
                                    BorderThickness="0"
                                    Padding="12,6"
                                    FontSize="12"
                                    Margin="0,0,8,0"
                                    Click="CategoryFilter_Click"
                                    Tag="{Binding Id}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <!-- Search Results -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      Padding="20,0,20,20">
            <StackPanel>
                <!-- Loading Indicator -->
                <Grid x:Name="LoadingIndicator"
                      Visibility="Collapsed">
                    <StackPanel HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                        <ProgressRing IsActive="True"
                                       Width="32"
                                       Height="32"
                                       Foreground="{StaticResource PrimaryBrush}"/>
                        <TextBlock Text="Searching..."
                                   FontSize="14"
                                   Foreground="{StaticResource TextSecondary}"
                                   Margin="0,8,0,0"/>
                    </StackPanel>
                </Grid>

                <!-- No Results -->
                <Grid x:Name="NoResultsIndicator"
                      Visibility="Collapsed">
                    <StackPanel HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                        <TextBlock Text="No results found"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimary}"
                                   Margin="0,0,0,8"/>
                        <TextBlock Text="Try different keywords or filters"
                                   FontSize="14"
                                   Foreground="{StaticResource TextSecondary}"
                                   TextAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <!-- Search Results Count -->
                <TextBlock x:Name="ResultsCountText"
                           Text=""
                           FontSize="14"
                           Foreground="{StaticResource TextSecondary}"
                           Margin="0,0,0,16"
                           Visibility="Collapsed"/>

                <!-- Results List -->
                <ItemsControl x:Name="ResultsList"
                              Visibility="Collapsed">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource CardBackground}"
                                    CornerRadius="12"
                                    Padding="16"
                                    Margin="0,0,0,12"
                                    Cursor="Hand">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Header -->
                                    <Grid Grid.Row="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding SenderName}"
                                                       FontSize="14"
                                                       FontWeight="SemiBold"
                                                       Foreground="{StaticResource TextPrimary}"/>
                                            <TextBlock Text="{Binding FormattedTimestamp}"
                                                       FontSize="12"
                                                       Foreground="{StaticResource TextSecondary}"/>
                                        </StackPanel>

                                        <Border Grid.Column="1"
                                                Background="{Binding PriorityColor}"
                                                CornerRadius="8"
                                                Padding="8,4"
                                                Visibility="{Binding HasPriority, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <TextBlock Text="{Binding Priority}"
                                                       FontSize="10"
                                                       FontWeight="SemiBold"
                                                       Foreground="White"/>
                                        </Border>
                                    </Grid>

                                    <!-- Message Text -->
                                    <TextBlock Grid.Row="1"
                                               Text="{Binding HighlightedText}"
                                               FontSize="14"
                                               Foreground="{StaticResource TextPrimary}"
                                               TextWrapping="Wrap"
                                               Margin="0,8,0,8"
                                               MaxLines="3"/>

                                    <!-- Tags -->
                                    <ItemsControl Grid.Row="2"
                                                  ItemsSource="{Binding Tags}"
                                                  Visibility="{Binding HasTags, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#f3f4f6"
                                                        CornerRadius="8"
                                                        Padding="8,4"
                                                        Margin="0,0,8,0">
                                                    <TextBlock Text="{Binding}"
                                                               FontSize="10"
                                                               Foreground="{StaticResource TextSecondary}"/>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Recent Searches -->
                <StackPanel x:Name="RecentSearchesPanel"
                            Visibility="Collapsed">
                    <TextBlock Text="Recent Searches"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimary}"
                               Margin="0,0,0,16"/>
                    
                    <ItemsControl x:Name="RecentSearchesList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource CardBackground}"
                                        CornerRadius="8"
                                        Padding="12"
                                        Margin="0,0,0,8"
                                        Cursor="Hand">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0"
                                                   Text="🕐"
                                                   FontSize="14"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,8,0"/>

                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Query}"
                                                   FontSize="14"
                                                   Foreground="{StaticResource TextPrimary}"
                                                   VerticalAlignment="Center"/>

                                        <Button Grid.Column="2"
                                                Content="✕"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Padding="4"
                                                FontSize="12"
                                                Foreground="{StaticResource TextSecondary}"
                                                Click="RemoveRecentSearch_Click"
                                                Tag="{Binding Query}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <!-- Empty State -->
                <StackPanel x:Name="EmptyStatePanel"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                    <TextBlock Text="🔍"
                               FontSize="48"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,16"/>
                    <TextBlock Text="Start typing to search messages"
                               FontSize="16"
                               Foreground="{StaticResource TextSecondary}"
                               TextAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
