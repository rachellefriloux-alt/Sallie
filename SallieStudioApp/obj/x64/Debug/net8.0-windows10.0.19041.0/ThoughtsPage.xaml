<Page
    x:Class="SallieStudioApp.ThoughtsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource WindowBackground}">

    <Grid Padding="20" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel>
            <TextBlock Text="Thoughts Log"
                       FontSize="28"
                       Foreground="{StaticResource TextPrimary}"/>
            <TextBlock Text="Internal monologue and cognitive processes"
                       FontSize="14"
                       Foreground="{StaticResource TextSecondary}"/>
        </StackPanel>

        <!-- Filters and Search -->
        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:ConnectionId='4' x:Name="SearchBox"
                     PlaceholderText="Search log entries..."
                                                        />

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button x:ConnectionId='6' x:Name="FilterAllBtn" Content="All"                      Tag="all"/>
                <Button x:ConnectionId='7' x:Name="FilterDebatesBtn" Content="Debates"                      Tag="debates"/>
                <Button x:ConnectionId='8' x:Name="FilterFrictionBtn" Content="Friction"                      Tag="friction"/>
                <Button x:ConnectionId='9' x:Name="FilterDecisionsBtn" Content="Decisions"                      Tag="decisions"/>
            </StackPanel>

            <Button x:ConnectionId='5' Grid.Column="2" Content="Export"                     />
        </Grid>

        <!-- Log Entries -->
        <Border Grid.Row="2"
                Background="{StaticResource CardBackground}"
                CornerRadius="6"
                Padding="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:ConnectionId='2' x:Name="EntriesList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#222"
                                    CornerRadius="8"
                                    Padding="12"
                                    Margin="0,0,0,8"
                                    BorderBrush="#374151"
                                    BorderThickness="1">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Text="{Binding Type}"
                                                   FontSize="11"
                                                   FontWeight="SemiBold"
                                                   Foreground="#a78bfa"/>

                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Timestamp}"
                                                   FontSize="11"
                                                   Foreground="#6b7280"
                                                   HorizontalAlignment="Right"/>
                                    </Grid>

                                    <TextBlock Text="{Binding Content}"
                                               FontSize="13"
                                               Foreground="#d1d5db"
                                               TextWrapping="Wrap"
                                               Margin="0,8,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

    </Grid>
</Page>

