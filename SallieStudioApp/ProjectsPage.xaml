<Page
    x:Class="SallieStudioApp.ProjectsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource WindowBackground}">

    <Grid Padding="20" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Projects &amp; Building Together"
                           FontSize="28"
                           Foreground="{StaticResource TextPrimary}"/>
                <TextBlock Text="Create projects, extensions, and build things together with Sallie"
                           FontSize="14"
                           Foreground="{StaticResource TextSecondary}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button Content="+ New Project" Click="NewProject_Click"/>
                <Button Content="+ New Extension" Click="NewExtension_Click"/>
            </StackPanel>
        </Grid>

        <!-- Content -->
        <Grid Grid.Row="1" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="320"/>
            </Grid.ColumnDefinitions>

            <!-- Projects and Extensions List -->
            <Border Background="{StaticResource CardBackground}"
                    CornerRadius="6"
                    Padding="16">
                <Grid RowSpacing="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Tabs -->
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button x:Name="ProjectsTabBtn" Content="Projects" Click="Tab_Click" Tag="projects"/>
                        <Button x:Name="ExtensionsTabBtn" Content="Extensions" Click="Tab_Click" Tag="extensions"/>
                        <Button x:Name="SkillsTabBtn" Content="Skills" Click="Tab_Click" Tag="skills"/>
                    </StackPanel>

                    <!-- Filter -->
                    <TextBox Grid.Row="1"
                             x:Name="SearchBox"
                             PlaceholderText="Search..."
                             TextChanged="Search_Changed"/>

                    <!-- List -->
                    <ScrollViewer Grid.Row="2">
                        <StackPanel x:Name="ItemsList" Spacing="8"/>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Details Panel -->
            <Border Grid.Column="1"
                    Background="{StaticResource CardBackground}"
                    CornerRadius="6"
                    Padding="16">
                <ScrollViewer>
                    <StackPanel x:Name="DetailsPanel" Spacing="12">
                        <TextBlock Text="Select an item to view details"
                                   Foreground="#6b7280"
                                   FontStyle="Italic"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

    </Grid>
</Page>
