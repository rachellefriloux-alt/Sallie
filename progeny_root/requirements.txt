# Core API Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
pydantic>=2.5.3
python-jose[cryptography]>=3.3.0

# Vector Database
qdrant-client>=1.7.1

# Data Processing
numpy>=1.26.3
scipy>=1.11.4

# Utilities
watchdog>=3.0.0
requests>=2.31.0
psutil>=5.9.8
PyNaCl>=1.5.0

# Audio & Text-to-Speech (Compatible with Python 3.12)
pyttsx3>=2.90  # Offline TTS, works with Python 3.12
gTTS>=2.4.0  # Google Text-to-Speech (online)
sounddevice>=0.4.6  # Audio I/O
soundfile>=0.12.1  # Audio file reading/writing
# Note: openai-whisper and Coqui TTS require older Python versions
# Use pyttsx3 for offline or gTTS for online TTS instead

# ML (Optional - only install if needed for embeddings)
# torch>=2.0.0
# sentence-transformers>=2.2.2

# System Tray / Desktop
plyer>=2.1.0
pystray>=0.19.5
Pillow>=10.2.0

# Testing
pytest>=8.0.0

# Dev Tools (Optional)
# black>=24.1.1
# ruff>=0.1.14
# mypy>=1.8.0
